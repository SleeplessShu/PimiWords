<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/landingOverlayRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_primary"
    android:clickable="true"
    android:focusable="true"
    android:visibility="gone">

    <!-- Guideline на 42% высоты — граница между перцем и контентом -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_mid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.42" />

    <!-- Перец строго в верхней зоне до guideline -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/animationIdleView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:lottie_autoPlay="false"
        app:lottie_loop="false"
        android:layout_marginTop="-160dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/guideline_mid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:lottie_rawRes="@raw/animation_base_loop" />

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/animationActionView"
        android:layout_width="0dp"
        android:layout_height="0dp"

        android:layout_marginTop="-160dp"
        app:lottie_autoPlay="false"
        app:lottie_loop="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/guideline_mid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:lottie_rawRes="@raw/animation_base_loop" />

    <!-- Текст сразу под guideline -->
    <TextView
        android:id="@+id/text"
        style="@style/Text.H2.Simple"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="-12dp"
        android:layout_marginBottom="12dp"
        android:gravity="center"
        android:text="@string/landing_language_select"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/guideline_mid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Список занимает всё оставшееся место -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvLanguages"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="24dp"
        app:layout_constraintTop_toBottomOf="@id/text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>